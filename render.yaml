services:
  - type: web
    name: cvo-pdf-server
    env: python
    buildCommand: "apt-get update && apt-get install -y fonts-dejavu-core && pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
